# 2026-08-19 工作日志

## 01:19 master.html 朗读「停止」无效 → 修复 stopId 未归一化 id
- 用户反馈：master.html 点「开始」朗读后，再点「停止」却没停止、还在朗读。
- **根因**：`audio-engine.js` 的 `stopKey`（即对外 `VocabAudio.stopId`）内部**没有**对传入 id 做 `normId` 归一化。`loop()/play()` 存入 `active` 的 key 是 `normId(id)`（即 `'s1'`），而 master.html 调用 `VocabAudio.stopId(sid)` 传的是**无 `s` 前缀**的 `sid`（如 `'1'`）→ `stopKey('1')` 去 `active['1']` 找不到节点 → 空操作 → 停止失效、朗读继续。
- **修复**：`audio-engine.js` `stopKey` 首行加 `key = normId(key);`（'1'/'s1' 都命中；已存的归一化 key 再 normId 幂等，不影响内部调用）。一行修复。
- **验证**（移动端 UA 无头实测，puppeteer-core + 本机 Chrome）：加载 master.html → 点开始 → `isActive('1')=true`、按钮「⏹ 停止」；再点停止 → `isActive('1')=false`、按钮「▶ 开始」→ `STOP_PASS ✅`（仅 2 个无关 404）。服务静态读盘 + no-store，修复即时生效，无需重启。
- 本次仅改 audio-engine.js（未提交 Git，待用户确认或随音色改动一并提交）。

## 01:19 用户询问"更好听、发音更标准的真人语音"替换
- 现状：`gen_audio.py` / `gen_one.py` 用**微软 Edge TTS（免费）**，音色 `en-US-AriaNeural`（Aria，Edge 高质量标准美音女声）；`master.json` 记 `"voice":"en-US-AriaNeural","voiceRate":"-10%"`。100 句 mp3 已生成。
- 待用户决策：① 免费换 Edge 神经音色（改一行 VOICE 重生成 100 句，推荐 AndrewNeural 标准男声 / EmmaNeural 温暖女声 / JennyNeural 友好 / GuyNeural 沉稳男声）；② ElevenLabs（最接近真人，需 API key + 付费）；③ OpenAI TTS（tts-1，需 API key，付费）；④ 保持 Aria。
- 已用 AskUserQuestion 询问方案。

## 01:25 选定①免费换 Edge 音色 → en-US-AndrewNeural（标准美音男声）
- 决策：用户选「换 Edge 免费音色」，采用默认推荐 **en-US-AndrewNeural**（清晰中性、跟读最标准）。
- 修改点：`gen_one.py` / `gen_audio.py` 的 VOICE、`gen_master_html.py` 与 `master.html` 页面文案（en-US-Aria→Andrew）、`master.json` 的 `"voice"` 记录。
- 重生成：写 `gen_audio_force.py`（全量重生成+重试，不走"仅缺失"逻辑），用 venv python（`envs/default/Scripts/python.exe`，edge_tts 7.2.8）跑完 **100/100 ok=100 fail=0**。
- 备份：旧 Aria 100 句存 `audio_backup_aria/`（已加 `.gitignore`，不入库，可一键还原）。
- 验证：服务器 s1.mp3 Content-Length=14400（新）、Last-Modified 新、`Cache-Control: no-store`；master.html 含 Andrew 文案；audio-engine.js 含 stopKey normId 修复。
- 提交：`f95287f`（108 文件，含 100 个新 mp3 + 代码），工作树干净。
- 注：想换女声(Emma/Jenny)或沉稳男声(Guy)随时说，改 VOICE 重跑 `gen_audio_force.py` 即可。

## 08:50 每日推送自动化 · Day 7（#21-25 新学）
- 服务：端口 3279 回写服务 /api/status=ok，常驻无需重启（任务文本写 8765 是过时信息，serve.py 实际绑 3279）。
- 驱动：run_daily.py（已修当日页命名为 day{YYYY-MM-DD}.html）。
- 选句：dayIndex=7≤20 → 新学 #21-25（离这多远 / 地图指路 / 拐角左转 / 附近药房 / 办理入住）；enh 在 master.json 预置完整，直接复用。
- 产出：day2026-08-19.html（5 句完整增强 + 自评 POST /api/mastery@3279）、master.html 重生成（已学 25/100 全含发音详情）。
- audio s21-s25 已存在跳过生成。nextExpansionDay=31（未触发）。

## 09:36 修复 run_daily.py 模板残缺：今日练习页缺导航/折叠/朗读控件
- 用户反馈（附截图）：day2026-08-19.html 缺少三个导航按钮（首页/已学回顾/日历）和发音详情折叠区域。
- **根因**：`run_daily.py` 内嵌的 HTML 模板是**精简版**（在之前 VocabAudio 迁移时未同步更新），缺少 `<nav class="pagenav">`、`.summary` 统计框、`.detailsBtn`/`.details.collapsed` 折叠详情、`.readwrap` 朗读循环控件、`.assess`/`.mbar` 掌握度进度条、语速选择器等。而 `push_day.py` 的模板是完整的。
- **修复**：将 `run_daily.py` 的 PAGE 模板整体替换为与 `push_day.py` 一致的完整版，补全所有 CSS 类 / HTML 结构 / JS 函数（toggleDetails/toggleAllDetails/loopSpeak/setSpeed/assess/setMbar 等）。同时补上 data_list 的 `mastery` 字段和模板变量 `__N__`/`__INTRO__`/`__TOTAL__`/`__POOL__`。
- **验证**：重跑 `run_daily.py` → day2026-08-19.html 含 pagenav(1)、detailsBtn(5)、readwrap(5)、loopSpeak、mbar 等全部功能；服务器 `Cache-Control: no-store` 即时生效。
- **教训**：项目有**两个**日练习页生成器（`push_day.py` 手动用 + `run_daily.py` 自动化用），各自维护独立模板。修改页面模板时必须**同步两者**，否则自动化会覆盖手动修复。已在 MEMORY.md 记录此铁律。
- 提交：`b8958ee`（4 文件 +520/-134）。

## 10:49 全站"自评掌握度"统一：master/review/calendar 也能交互自评并跨页同步
- 需求：用户要求自评掌握度不只在当日练习页可点，其他页面也应同步展示并可选。
- **新增 `mastery.js`（共享模块）**：`VocabMastery.assess(sid,action,btn)` / `setBar` / `refreshAll` / `widgetHTML` / `getMap` / `num`。
  - 交互控件统一结构：`<div class="assess" data-mid="N">…<div class="mbar" id="mbar-N"><div class="mfill">…</div></div><span class="mbadge" id="mb-N">…</span><button class="c/f/u" onclick="VocabMastery.assess('N','clear|fuzzy|unknown',this)">…</button><span class="astat" id="as-N">`。
  - `assess` 走 `POST /api/mastery {id:N, action}`，回写 master.json；`refreshAll` 走 `GET /api/mastery` 把服务端最新值应用到全部 `mbar-N`（加载时 + 各页按需调用）→ 各页互相同步。
  - id 约定：数字字符串（'4'），带不带 's' 前缀都兼容（num() 提取数字）。
- **改造点**：
  - `gen_master_html.py`：卡片静态 `.mwrap` 徽章 → 交互式 `.assess` 控件（含 CSS）；JS 的 `renderBar/syncMastery` 改为调用 `VocabMastery.refreshAll()`（保留同步按钮+15s 轮询）；引入 `mastery.js`。
  - `cards.js`：`renderSentenceCard` 的 `.scfoot` 由原来的静态 `masteryBadge` 改为 `VocabMastery.widgetHTML(s.id)`（带 `typeof VocabMastery` 兜底）；`cards.css` 加 `.assess` 样式。
  - `gen_views_html.py`：review/calendar 模板引入 `mastery.js`（在 cards.js 前），加载完成 / `showDay` 渲染后调用 `VocabMastery.refreshAll()` 同步。
  - index.html 是入口菜单（不列具体句子），保持原样（仅显示回写服务状态）。
- **验证**（移动端 UA 无头实测 puppeteer-core）：
  - 总览点 s1「认识+1」→ 服务端 3/5、stat「已回写 ✓」；切到回顾页 s1 即时显示 3/5（同步）；回顾页点「不认识-1」→ 还原 2/5；MASTERY_PASS ✅。
  - 日历页点「最近练习」→ 当日句式卡片出现自评控件；点 s16「认识+1」→ 1/5 回写、再「不认识-1」→ 还原 0/5；CAL_PASS ✅。
- **关键坑（务必记）**：`serve.py` 启动时 `import gen_views_html` 把生成器**锁进内存**；`POST /api/mastery` 成功后会调用 `gen_views_html.main()` 重建 review/calendar。若服务未重启、而生成器已被改（如新增 mastery.js 引用），旧内存模块会用旧代码覆盖刚生成的文件 → 多页自评"看似没生效"。**已在 serve.py 改为 `importlib.reload(gen_views_html)` 后再 `main()`**（每次回写用最新源码）。改任何生成器后必须**重启 vocab-serve 服务**（否则仍跑旧内存模块）。
- 提交：待执行（本次改动含 mastery.js 新文件 + 4 个生成器/共享文件 + 重生成的 master/review/calendar.html + serve.py）。

