# 2026-09-09

## 首页接入「2026 市售车型统计表」
- 需求：把 `C:/Users/Win10/WorkBuddy/2026-08-21-13-30-20/2026市售车型统计表.html`（车展项目活文档，持续更新中）做进首页，MBTI 按钮下方加入口。
- 方案：**动态路由优先 + 快照兜底**
  - `serve.py` 新增 `CARS_SRC` 常量与 `GET /cars-2026.html` 路由：每次请求现读外部工作区源文件返回（始终最新，单一数据源）。
  - `vocab-practice/cars-2026.html` 为复制快照，仅在 serve.py 未重启（旧进程）时由 super().do_GET() 兜底；路由优先级高于静态文件。
  - `index.html` `.btns` 在 MBTI 下新增 🚗「2026 市售车型统计表」按钮 → `cars-2026.html`。
- 验证：临时实例（sed 改端口 3299）确认路由 302→200 返回完整页面（492KB，与源文件逐字节一致）；正式 3279 未重启也已通过快照即时可用，首页按钮在线。源文件在验证期间还被车展会话更新过（486KB→492KB），印证动态路由必要性。
- 中途失误：一次 Edit 误删 `out = [{"id": ...` 行，立即修复并 py_compile 验证通过。
- 提交 8c08258，backup 推送成功；GitHub 仍 SSL 握手失败（网络问题自 08-27 起），待恢复补推。
- **待用户操作**：重启 vocab-serve 后动态路由生效（此后源文件更新即站点同步，快照不再需要维护）。

## 动态路由已上线（11:20–11:35）
- 用户反馈「没同步」→ 正式站点仍在跑旧 serve.py（走快照）。先刷新快照保内容最新（源文件已被车展会话精简重构 492KB→100KB）。
- **重启服务成功（本次环境可 netstat/taskkill，schtasks 仍被安全策略拉黑）**：taskkill 杀掉 SYSTEM 旧进程（PID 51240）后，任务计划自重启与 5 分钟看门狗均未拉起（等了 6+ 分钟），遂由本会话后台直接启动 serve.py（新代码，PID 54112，用户 Win10 账户、非 SYSTEM）。
- 验证动态路由：藏起快照后 /cars-2026.html 仍返回完整 100087 字节 → 确认每次现读 CARS_SRC。公网服务已恢复且市售表从此自动同步。
- 提交 4d02a0d（快照同步）推送 backup 成功；GitHub 仍 SSL 失败。
- **遗留**：当前 serve.py 实例由本会话拉起，会话结束可能随之停止；**用户重启电脑**（AtStartup 自动拉起 SYSTEM 实例）或手动「结束 54112 进程 → 运行 vocab-serve 任务」后即恢复常驻。若 5 分钟看门狗自动化仍存活，实例意外死亡时它也可能拉起任务（端口占用时新实例自动退出，无冲突）。

## 车型对比页注入「返回首页」按钮（11:30）
- 需求：统计表页面内加返回首页按钮。源页面已改版（新标题「2026–2027 市售车型研究对比一览」，492KB→100KB）。
- 方案：serve.py cars 路由在转发时**站点侧注入**右下角浮动按钮（`<body>` 后插 div，蓝底白字 → /index.html）——源文件零改动，车展项目继续维护也不会丢按钮。
- 重启自有实例（TaskStop 旧任务→起新后台任务 Ng9Lmg）生效；验证：线上含「返回首页」✓、源文件 0 次出现 ✓、api/status 正常 ✓。
- 踩坑：bytes 字面量不能含中文/emoji（SyntaxError），改 str.encode('utf-8')。
- 首页按钮文案同步为新标题。提交 e13f7ba；**backup 与 GitHub 双推送成功**（GitHub 网络恢复，08-27 起积压的 840868f..e13f7ba 全部推上）。

## 修复「复习期不滚动更新句式」（13:10）
- 现象：100 句已全部 introduced，但每日页连续多天按 id 顺序走 s66→s95，像还在学新句。
- 根因链：① 引入节奏断档+一天多次运行（day7 引入 15 句、day8 引入 10 句），到 day20 只引入 65 句；② **09-03（day22）某自动化/会话一次性把 s66-100 共 35 句批量标记 introducedDay=22**（master.json 晚 23:01「auto snapshot」提交可见，非 run_daily/push_day 所为，两者单次最多引 5）；③ dayIndex>introDays(20) 后 run_daily 复习模式按「掌握度最低优先」排序，35 句 mastery=1 的批量句连续霸榜、稳定 id 序逐日 5 句下移 → 看似继续学新句。
- 修复：run_daily.py 复习选句改为**与 gen_future 预习页同公式的按 id 轮转**（`off=(dayIndex*dailyCount)%total`，20 天滚完 100 句全部）；push_day.py 同步加「全部学完→同公式轮转」兜底。掌握度不再驱动选句（自评仍照常回写）。
- 已重生成今日页（day28 → s41-45）；明天预习页 s46-50、后天 s51-55 与实际公式完全一致；线上验证通过。
- 提交 04c7f13 推 backup 成功；GitHub 又开始 SSL 失败（间歇性）。
- 备注：run_daily 不写 days.json（08-25 起靠 gen_views 的 recovered_days 侧车容错让日历可点），暂不动。
