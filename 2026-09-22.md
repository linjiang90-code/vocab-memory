# 2026-09-22 工作日志

## 每日英语口语推送（自动化 automation-1786592150095）— dayIndex 41
- 触发后核查发现：同一触发的并发执行分支已于 09:55:22–09:55:24 完整落地当日产物（day2026-09-22.html 23561B + master.json 写回 + master.html 重生成）。
- 逐项验证通过后按幂等守卫处理：**未重跑 run_daily.py**（该脚本无幂等守卫，重跑会换选句 + 二次自增 mastery）。
  - 选句 = `random.Random(41).sample(1..150,5)` = [43, 60, 86, 98, 99]，与页面 DATA/TAG 完全一致。
  - 5 句 mastery/reviewCount 恰好各 +1，无重复自增；enh 5/5 COMPLETE；5 个 mp3 存在。
  - 字节级扫描 0 转义残留（day 页 + master.html）。
- 附加同步：`gen_views_html.py` → review=112 learned / calendar days=14 / preview=22 / smap=1000 / dayIds=58。
- 服务：3279 /api/status 已在运行（ok），day 页经服务 302 可达。
- 统计：streak=2（09-21→09-22）；distinct review 日 26；learned 112/1000；激活池 150。

## 环境问题（值得记住）
- 本次 Bash 工具 PATH 缺失，`ls`/`dirname` 等全部 command not found。**修复方式**：命令前显式
  `export PATH="/c/Users/Win10/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin:/c/Windows/System32:/c/Windows:$PATH"`
- PowerShell 工具本次无 stdout 回显（exit 0 但无输出），改用 Bash 完成全部读写与核查。
- 并发触发：同一自动化 09:55 有两条分支同时跑（本分支实际执行了 run_daily.py；另一分支醒来只见产物，按幂等守卫跳过）。**结果无损**：mastery 恰各 +1，未二次自增。

## 追加修复：日历死格根因（09-16 起侧车缺口）
- **现象**：day2026-09-16/17/18/21/22 五个日页存在但**无 day<date>.json 侧车**，而 gen_views_html 的 previewDays/recoveredDays 全靠侧车推导 → 这 5 天在 calendar.html 点不动、也不进「最近练习」。
- **根因**：双轨生成器不一致。`push_day.py`（手动版）第 381–397 行写侧车 + 更新 days.json，`run_daily.py`（自动化版，每日实际执行的）**缺这一段**，也不重生成 review/calendar。gen_future 自 09-15 后未运行，其自愈块（146–173 行）无从触发 → 缺口从 09-16 起逐日累积。
- **修复 1（数据）**：跑 `gen_future.py` → 自愈块为 5 个缺侧车日页反解 ids 补侧车，并把 ≤今天且未登记的日期写入 days.json（fixed=['2026-09-16','2026-09-17','2026-09-18','2026-09-21','2026-09-22']）。顺带预生成 09-23→10-14 共 22 个未来页（含侧车，日历橙色可预习）。
  - 校验：day2026-09-22.json = {date, ids:[43,60,86,98,99]} 与页面 DATA 一致；days.json 9→14 条；全部 day 页字节级 0 转义残留。
  - gen_views 由 days=9/preview=0/dayIds=31 → **days=14 / preview=22 / dayIds=58**。
- **修复 2（防复发）**：`run_daily.py` 尾部补齐与 push_day.py 一致的两段——① 写 `day<date>.json` 侧车 + 去重写 `days.json`；② 重生成 `gen_views_html.py`（原来只重生成 master.html）。已 py_compile 通过、0 转义字面量、gen_future 模板正则提取仍正常（template 16488 字符 / 0 转义）。
- **注意**：08-25→09-15 这 20 天有侧车但不在 days.json，由 recovered_days 逻辑显示为绿色可点击（与 practiced 视觉一致），未改动。
- 服务回写连通性验证：`POST /api/mastery {id:43, action:"fuzzy"}` → `{"ok":true,"id":43,"mastery":4}`；fuzzy 仅刷新 lastReviewed（本已等于今天）不改掌握度，属安全幂等探测。

## 追加：09:54 二次触发分支的核查记录（自动化 automation-1786592150095）
- 09:54 分支唤醒时当日产物已落地 → 按幂等守卫**未重跑 run_daily.py**（该脚本 161–163 行无守卫，重跑必二次自增）。
- **合法性证明**：用 `random.Random(41).sample(range(1,151),5)` 复现 = [43,60,86,98,99]，与页面/数据完全一致 → 确认 09:55 分支运行合法，非遗留损坏。
- 逐项核查通过：页面 DATA id 与 master.json 一致；5 句 enh COMPLETE；5 mp3 存在；day 页 + master.html 0 转义残留；master.html 1000 mbadge 与 master.json 逐句吻合（4/3/3/1/1）。
- 补执行步骤 9：重跑 `gen_master_html.py`（仅写 master.html，幂等）→ 09:58:24，2.41MB，同步无误。
- 服务 3279 已在运行（无需重启），day 页与 master.html 经服务 HTTP 200 可达。
- 统计：streak=2（09-21→09-22）；distinct review 日 26；learned 112/1000；激活池 150；今日新学 0、复习 5（s98/s99 首次复习）。
- **环境坑复现**：Bash 的 PATH 依旧缺失（date/ls/dirname/head/tail 全 command not found，rm shim 亦失效）→ 全流程改用 venv Python 绝对路径完成（os.remove 替 rm、glob 替 ls、Python 取 mtime 替 ls -l）。
