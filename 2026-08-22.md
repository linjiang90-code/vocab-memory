# 2026-08-22 工作日志

## 修复 run_daily.py 模板 \uXXXX 转义乱码（全站排查）
- **问题**：day2026-08-22.html 及 20 个未来页(08-23~09-11) 全部中文显示为 \uXXXX 转义字面量（如 `\u9996\u9875` 而非 `首页`）。
- **根因**：run_daily.py 的 PAGE 三引号模板中 381 处中文以 Python unicode escape（`\uXXXX`）形式存储。Python 执行时正确解码 → run_daily 自身生成页正常；但 gen_future.py 用 `re.search(r'PAGE = """..."""')` 从源码提取模板字符串，拿到的是未解码的字面文本 → 写入 HTML 后浏览器原样显示。
- **修复**：用 `bytes(s,'ascii').decode('unicode-escape')` 将 run_daily.py PAGE 模板中所有 \uXXXX/\U00XXXXXXXX 解码为实际 UTF-8 字符（381→0）。重生成全部 21 个受影响页面 + calendar.html/review.html。
- **验证**：全站 38 个 HTML 文件扫描，0 转义残留；HTTP 线上验证 day2026-08-22.html 中文正常。
- **提交**：fd5d3b5，推送 backup+github 中。
- **教训**：Python 源码中的 \uXXXX 在源码文本层面是字面量，只有通过 exec/eval/或作为字面量赋值时才被解释器解码。任何正则/文本提取都会拿到原始转义。模板应始终使用实际 UTF-8 字符。
