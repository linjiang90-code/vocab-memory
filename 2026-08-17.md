# 2026-08-17 工作日志

## 修复：回写服务断连 + 今日练习未推送
- 现象（用户 09:41 反馈）：打开练习页提示「回写服务未连接」，且「今日练习未推送」。
- 根因：本地 serve.py（端口 8765，掌握度回写 API）后台进程在会话/进程结束后已退出；每日 10:00 推送自动化虽自带「第0步确保服务运行」，但仅覆盖推送时段，非推送时段（如早上 10 点前）服务死亡后无人拉起，导致用户随时打开页面回写不可用。中间 8/15、8/16 的自动推送也因服务/会话问题漏推。
- 处置：
  1. 立即后台重启 serve.py → 端口 8765 OPEN，`/api/status` 返回 `{"ok":true}`（本次会话后台 task O9IpZi）。
  2. 手动补推今日 `push_day.py 2026-08-17` → 生成 day2026-08-17.html，选中 s11-15（未学优先顺延，未重复已学的 s1-10）。master.json 累计引入 15 句。10:00 推送自动化若再触发为幂等（s11-15 introducedDay 已=5，不会重复 +1）。
  3. 新增自动化 `automation-1786931245986`「vocab 回写服务保活(每日09:30)」：每日 09:30 检查 8765，dead 则后台拉起 serve.py 并验证 API，已活则跳过。与 10:00 推送、23:00 提交形成保活/提交网。
- 已提交+推送 vocab-practice 仓库（2cab3f7：master.json + day2026-08-17.html + master.html 重生成）。

## 当前状态
- 回写服务：8765 运行中（task O9IpZi）。
- 学习进度：累计引入 15 句（s1-s15）。s1-5 掌握度1(8/13)、s6-10 掌握度1(8/14)、s11-15 掌握度1(8/17)。s16-20 待推。
- 自动化（均 ACTIVE）：09:30 保活(1786931245986) / 10:00 推送(1786592150095) / 23:00 提交推送(1786609176614)。

## 经验
- 后台 serve.py 进程会随 agent 会话/进程退出而终结，不能假设「起过就一直活」。需独立保活自动化（本案加 09:30 检查），而非仅靠 10:00 推送拉起。
- push_day.py 选句用「未引入优先取前 dailyCount」，漏推几天不会重复已学句，只会顺延学到的新句；introducedDay 标记的是实际推送当天的 dayIndex。

## 10:18 重启回写服务（二次断连）
- 现象：用户要求「重启回写服务」，检测 8765 = CLOSED。
- 根因（关键教训）：**端口被两个 serve.py 实例同时 LISTENING**（PID 4604=09:41 那次重启的残留进程 O9IpZi，PID 19428=本次刚启动的），导致请求被混乱分发/立即 CLOSE_WAIT 关闭。`HTTPServer` 单线程、未设 SO_REUSEADDR，两个实例都活着时新版绑定成功但端口争用。
- 处置：① TaskStop 杀掉本次后台任务 9Pahrp；② PowerShell `Stop-Process -Id 4604,19428 -Force` 清掉两个冲突进程；③ 确认 `netstat` 无 8765 LISTENING（端口干净）；④ 重新启动单个干净实例（task z5cwj6）。验证：8765 OPEN、`/api/status` 返回 `{"ok":true}`、**仅 1 个进程 LISTENING**。
- 教训：**重启回写服务前必须先杀掉旧监听进程**，否则会双实例冲突。建议把「重启」流程固化为：先 taskkill 端口占用 → 再启动。09:30 保活自动化也应加「检测到已有监听进程则先不重复拉起」的防双开逻辑（目前仅 curl 探活，不会双开；但手动多次重启会冲突，需注意）。

## 11:10 回写服务未连接（三连断 + 根治）
- 现象：用户第三次报「回写服务未连接」。检测 8765 = CLOSED（端口干净，无残留进程，非双实例）。
- 真因（关键！）：服务进程活着、端口 OPEN，但 `HTTPServer` 是**单线程**，浏览器同时请求「大 HTML 页面 + /api/status」时，页面请求占用线程、API 请求排队，页面 JS 的 fetch 8 秒超时后误报「未连接」。日志里 `/api/status 200` 其实正常返回，只是被排队延迟。
- 根治：serve.py 把 `HTTPServer` 换成 `ThreadingHTTPServer` + `daemon_threads=True`（每请求独立线程，页面与 API 互不阻塞）。重启流程：TaskStop 旧实例 → PowerShell 清残留监听 → netstat 确认干净 → 起新实例（task 31FR2G）。
- 验证：4 线程并发发 5 个请求（含 day2026-08-17.html 大文件 + 3 次 /api/status + master.html）**全部 200、零超时**，确认根治。
- 已提交+推送 vocab-practice（43ef860：serve.py 多线程化）。
- **结论**：「回写服务未连接」至此有三层防护——① 保活自动化（后调整为 08:55，推送前就绪）+ 推送自动化第 0 步自动拉起；② 端口干净再启动防双实例；③ 多线程消除偶发超时。后续若再报未连接，优先用并发探测（多线程 urllib 模拟）验证，而非单点 curl。

## 自动化时间调整（用户要求：推送改为每天 9 点）
- 推送自动化 automation-1786592150095「每日英语口语推送」：rrule 由 `BYHOUR=10` 改为 `BYHOUR=9`（09:00 触发），prompt 内约束「每天 09:00 触发」同步改好。
- 保活自动化 automation-1786931245986「vocab 回写服务保活」：由 09:30 提前到 `BYHOUR=8;BYMINUTE=55`（08:55），确保 9 点推送前服务已就绪，且覆盖 8–9 点用户打开练习页的时段。
- Git 提交：本次仅改自动化配置（存于 workbuddy.db，非 vocab-practice 代码文件），vocab-practice 仓库无需提交；变更仅记入本记忆仓库。

## 16:06 第三次「回写服务未连接」——根因是僵死占端口
- 现象：用户第四次报未连接。netstat 显示 8765 `LISTENING`（PID 30448，单进程），但 Python `urllib` 探活 `GET /api/status` 报 `Remote end closed connection without response`——**进程监听着端口却不响应、直接关闭连接**（比单线程排队超时更隐蔽，端口没释放所以旧保活逻辑「端口占用就跳过」会误判为已存活）。
- 处理：TaskStop 旧后台任务（已被回收，id 失效）→ PowerShell `Stop-Process -Id 30448 -Force` → netstat 确认 8765 无 LISTENING（端口干净）→ 起新多线程实例（task `lvsiWA`）→ 并发 5 请求（day2026-08-17.html + 3×/api/status + master.html）**全 200、零超时、单进程**，确认根治。
- **加固保活自动化 automation-1786931245986**：① rrule 由单点 08:55 改为每天 4 时点 `BYHOUR=8,12,18,22;BYMINUTE=55`（覆盖白天僵死窗口，配合 09:00 推送）；② 第 3 步改为「探活不可达→先用 PowerShell 杀掉占用 8765 的 LISTENING 进程→确认端口释放→再启动 serve.py→Python 验证能真正响应（非僵死）」。原保活只 curl 探活且「占用就跳过」，无法应对僵死，现已修正。
- 结论：回写服务防护升级为——① 多时点保活(08:55/12:55/18:55/22:55) ② 僵死识别+强制清理端口再重启 ③ 推送第0步兜底拉起 ④ 多线程不阻塞。后续白天再报未连接，保活会在最近时点自动清理僵死并重启；若仍连不上再手动让我重启。

## 16:30 加 GitHub 云端备份
- 用户要求为两个 Git 仓库加 GitHub 远程做云端备份。账号 linjiang90-code。
- 已建两个**公开**仓库：vocab-practice（学习系统）、vocab-memory（记忆/进度）；默认分支已设为 master（与本地一致）。
- 两本地仓库均加 `github` 远程（URL 内嵌 PAT，自动化可无交互推送）。PAT 用经典令牌 `ghp_`、scope=repo；前两次用户给的 `github_pat_` 细粒度令牌缺 repo 权限 → 建仓/推送均 403。
- 凭据另写入 `~/.git-credentials`（credential.helper=store）双保险。
- 23:00 自动化 automation-1786609176614 已更新：先 `git push backup`（本地）再 `git push github`（云端）；github 临时网络失败仅记录不中断。
- 关键坑：git push 在 Bash 沙箱内访问不了外网（curl 也需 dangerouslyDisableSandbox=true），首次推送卡 13 分钟无连接；已放开沙箱重推成功（两仓库均 master→master）。
- 教训：凡涉及外网 git push / curl，必须 dangerouslyDisableSandbox=true；细粒度 PAT 默认无 repo 权限，建仓+推送需 classic ghp_(repo) 或在细粒度显式授权 Repository creation + Contents。

## 23:00 重启回写服务
- 用户要求「重启回写服务」。检测 8765 = LISTENING(PID 15764) + venv python PID 729(16:08 起)。按流程：PowerShell Stop-Process 强杀 15764/729 → netstat 确认无 LISTENING(仅 TIME_WAIT) → 起新多线程实例(task dOo01f) → 并发 6 请求(4×/api/status + master.html + day2026-08-17.html)全 200、单进程(PID 37692)、零超时，确认真响应非僵死。
- 结论：当前服务健康，端口 8765 正常回写；后续白天再报未连接，保活(08:55/12:55/18:55/22:55)会在最近时点自动清理僵死并重启。

## 23:10 搭配内网穿透做外网远程访问
- 用户给公网入口 `125.71.210.3:3279`，并说明路由器端口映射（虚拟服务器）目标是内网 `192.168.4.97:3279`（已确认 192.168.4.97 是本机网卡 IP）。
- 原 serve.py 监听 `127.0.0.1:8765`，端口/地址都接不住映射流量 → 外网 Connection refused。
- 改造（端口 8765→3279 全量统一）：
  1. Python 批量替换 vocab-practice 下所有 html/py 的 `8765`→`3279`（含页面回写兜底地址 API_PORT / `http://127.0.0.1:3279`，共 10 文件：serve.py、gen_master_html.py、push_day.py、push_today.py、run_daily.py、index.html、master.html、day2.html、day2026-08-13.html、day2026-08-17.html）。
  2. serve.py 监听地址 `127.0.0.1`→`0.0.0.0`（覆盖 192.168.4.97 与回环），端口 3279。
  3. 停旧 8765 进程(PID 37692)、清端口，起新实例(0.0.0.0:3279, task 2cUwcG, PID 37772)。
  4. netsh 加防火墙入站允许规则 `vocab-practice-3279`（TCP 3279）。
- 验证：本机 `127.0.0.1:3279/api/status` ok；`192.168.4.97:3279/master.html` 返回 370543B 完整页面；回写 POST /api/mastery 功能正常(fuzzy→unknown 往返)。本机 curl 公网 `125.71.210.3:3279` 因 NAT 回流限制连不上（正常假象），需用户用手机流量(关 WiFi)实测确认外网可达。
- 保活自动化 automation-1786931245986 端口同步 8765→3279（4 时点 08:55/12:55/18:55/22:55 探活 127.0.0.1:3279，dead 则杀 3279 占用进程重启；prompt 注明本机测公网地址的 NAT 回流假象，判断可用性只用 127.0.0.1:3279 探活）。
- 访问入口：**本地** `http://127.0.0.1:3279/`，**外网** `http://125.71.210.3:3279/master.html`（页面回写走相对路径 /api/，自动跟随 host:port，本地外网通用）。
- 风险/提醒：① serve.py 暴露公网且无人认证，任何人可改 master.json（仅学习数据，风险低，建议后续加 Basic Auth 或 token）；② 家庭宽带公网 IP 125.71.210.3 多为动态，重连可能变，需 DDNS 或用户自行留意；③ 23:00 自动化仅 git 提交，端口映射属路由器配置不受影响。
## 23:17 回写服务加 HTTP Basic Auth（公网暴露后上锁）
- 服务启用 HTTP Basic Auth：GET/POST/HEAD 全鉴权，凭据存本机 serve_auth.json（已 gitignore，不进公开仓库）。serve.py 每次请求读文件，改密后无需重启即生效；保活自动化无需改。
- 23:22 用户将账号改为 `lin` / 密码 `l234567`（serve_auth.json 更新，重启验证：新账号→200、旧 vocab 账号→401、无凭证→401；带新账号 master.html→370543B）。远程访问指引.html 已同步更新账号框。
