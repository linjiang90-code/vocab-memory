# 2026-09-10

## 语料扩展至 1000 句 + 每日随机推送 + 30 天解锁机制
- 需求：常用口语/旅游句式扩到 **1000 句不重复**；**每天 5 句随机推送**；**每 30 天 +50 新句**（100→150→200…）；全局同步检查。
- 语料：新写 900 句（sentences_pool_1..9.py，管道分隔紧凑格式 en|zh|theme|category|length|关键词|音标|词性释义），`expand_corpus.py` 幂等合并进 master.json（ids 101-1000，introduced=False）。校验：总数恰 900、与原 100 句及内部 **0 重复**。类别沿用「主题/子类」中文风格（机场/值机、餐厅/点餐、寒暄/重逢、情绪/惊讶等）。
- 选句机制（三处同公式，已验证一致）：`rng=random.Random(dayIndex)` + `sample(可用ids,5)`——**同日幂等**（重跑/手动跑结果一致）、**跨日随机**；引入期仍按 id 顺序。可用句库 = `meta.activeCount`。
- 扩展机制：改为 `activeCount` 每 30 天 +50（while 循环可补漏），封顶 1000；旧 NEW_SENTENCES 逐批追加机制废弃。Day 31（09-12）首次扩展至 150。
- 同步点全改：run_daily.py（扩展+选句+import random）、push_day.py（选句，删除 already 分支）、gen_future.py（active_at() 模拟未来激活+同公式选句）、gen_master_html.py（头部改为「当前可用 X / 语料共 1000」、d-empty 文案改为如实描述）、index.html（标题/副标题/按钮文案）、master.html/review/calendar 重生成。
- 验证：模拟 day 28/29/30/31/45/61/300 选句均随机且 Day31 起含新句；今日页（day29）实际选句与模拟完全一致 [10,45,71,77,78]；明天预习页重生成匹配；master.html 2.45MB 正常服务；/api/mastery 返回 1000 句正常。
- 音频：gen_one.py 管线验证通过，已预生成 **101-150** 全部 50 个 mp3（0 失败，3 分钟）；151+ 由 run_daily 选中时按需自动生成。
- 提交 3 笔（语料+机制 / 音频 / 等），推 **backup 与 GitHub 双成功**（5d6ebe2）。
- 注意：900 新句暂无 enh 详细标注（整句音标/变体/场景/语法/发音），总览页如实显示「暂未撰写，逐步补充」；后续可按批次补写。

## ENH 标注回填 101–150（Day 31 激活批次）
- 新增 `enh_batch_101_125.py` / `enh_batch_126_150.py`（ENH 字典：fullIpa/3 变体/3 场景/语法/发音，格式对齐原 100 句）+ `enh_merge.py`（幂等合并：仅 fullIpa 为空才填）。
- 合并结果：filled=50 skipped=0，master.json 现在 **150/1000** 句有完整 enh；重跑幂等验证通过。
- master.html 重生成（2.53MB）：150 卡片带「整句音标」，850 张显示占位文案；线上验证一致。
- 校验：s101/s150 有 enh、s151 为空、meta（activeCount=100, nextExpansionDay=31）正确。
- 提交 f55d49b。推送 backup/GitHub 时遇到审批超时被拦截，**推送未完成，需用户手动 push 或下轮批准**。
