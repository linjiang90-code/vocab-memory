# 2026-08-20 工作日志

## 今日推送修复（用户问"今日推送呢"）
- 现象：day2026-08-20.html 未生成（自动化每日9:00跑但今日页缺失）。
- 根因：自动化「每日英语口语推送」第0步探活地址写成 **http://127.0.0.1:8765/api/status**（错误端口），真实服务跑在 **3279**。误判"服务不可达"并尝试起错误端口服务，导致第6步生成页面未真正完成。
- 修复：用已修好的 run_daily.py 立即补生成 day2026-08-20.html（dayIndex=8，新学 s36-s40）；并把自动化 prompt 端口 8765→3279，明确"第6步用 run_daily.py 生成、勿手搓 HTML"。
- 每5分钟保活自动化端口正确（3279），无需改。
- 验证：今日页 200、含 pagenav/折叠/语速1倍·0.8倍·0.7倍·0.6倍/🔊🐢🔁/自评控件，s36-s40 音频齐全。

## 把上一轮未提交改动一起提交推送
- 上轮（audio-engine 兜底定时器重写 + 语速统一1/0.8/0.7/0.6 + review/calendar 补 audio-engine.js 引入）改动当时未提交，本次一并提交 f2fc498（17文件 +847/-567，含新增 day2026-08-20.html）。
- backup 推送成功（08af796..f2fc498）；github 首次 Connection reset，重试后台推送中（大仓库含100 mp3）。

## 铁律补充
- 自动化 prompt 里凡涉及服务端口必须写 **3279**（勿写 8765）；每日推送务必用 run_daily.py（与 push_day.py 模板同步），不要手搓 day 页 HTML。

## 预生成未来22天 + 日历提前预习（用户："把后面22天提前生成，也能在日历里提前学"）
- 新增 `gen_future.py`：为明天起 N 天（默认22，08-21..09-11）生成静态 day<date>.html + day<date>.json 侧车，**不改动 master.json 的 introduced/mastery**（进度仍由每日 run_daily.py 在真实日期接管，届时同款内容无缝覆盖）。
- 模板来源：gen_future.py 用正则直接从 run_daily.py 提取 PAGE 模板字符串 → 与每日自动化模板自动保持一致（避免"双轨/三轨"漂移）。预览页额外注入 `🔓 预习模式` 横幅（真实自动化页不含）。
- 选句逻辑与未来自动化一致：首个未引入句=46，从 id 顺序每5句一批（新句日 dayIndex<=20 取 s46→s100；超总句数/超 introDays 进入复习预览，按 id 轮转取5句，使不同未来日展示不同句式）。
- 日历增强：gen_views_html.py 从 day<date>.json 侧车识别"晚于今天且不在 days.json"的日期为 previewDays（22个）；calendar 网格橙色高亮且可点击 showDay 直接学习；页头「可预习」快捷条；sMap 含全100句，预习页与正式页同享音频/自评/详情。
- 顺手修复：补齐 08-19/08-20 缺失侧车并登记 days.json（历史 run_daily 未写侧车所致）。
- 验证：HTTP 200；served calendar.html 含 previewDays=22（08-21..09-11），08-21→[46,47,48,49,50]；day 页 s46-s50 含 fullIpa/variants/audio。
- 提交 097a921，推送 backup+github（后台）。

