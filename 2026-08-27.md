# 2026-08-27

## 新增 MBTI 性格自测功能
- 需求：首页加「测 MBTI」按钮 → 跳转专门自测页（93 题，算出类型）。
- 产出：
  - `mbti.html`：自包含纯前端 93 题自测。题目按 EI/SN/TF/JP 均衡分布（24/24/22/23=93），每题 5 级同意度量表；计分：favored 极 += 分值、对立极 += (6-分值)；输出 4 字母类型 + 各维度百分比条形 + 16 型 nick/描述 + 维度说明；localStorage 暂存答案。
  - `index.html`：`.btns` 内新增指向 `mbti.html` 的 🧠 按钮（风格与现有一致）。
- 验证：本地 Grep 确认 93 题 + 维度拆分；node --check 语法 OK；node 跑计分逻辑（全5→ESTJ、全1→ESTP、混合→ESFJ）均合法；线上 mbti.html 302 ?v= 缓存击穿正常。
- 提交 840868f，backup 推送成功；GitHub 推送因网络重置/无法连接暂失败，待网络恢复补推。
- 说明：题目为自设计的 93 题量表（基于 MBTI 四组二元维度），非官方/临床工具，页面已注明。

## MBTI 结果页增强（同日）
- 需求：输出结果要含「详细介绍 + 结果解析（优势/盲区）+ 人生与生活指导」。
- 改动 `mbti.html`：把原 NICK/DESC 两表合并为 `PROFILE`（16 型，每型含 nick/desc/intro/strength/blind/guide[3]）；`showResult()` 渲染新增「性格详细介绍」「结果解析·你的优势与盲区」「人生与生活指导」三块 + 配套 CSS；底部说明补全「非临床/官方工具」。
- 验证：node 加载真实脚本 + DOM 桩，16 型全部渲染含三段且无 `undefined`（ALL PASS）。
- 提交 06992ef，backup 推送成功；GitHub 仍因 Connection was reset 失败，待网络恢复补推（或手动 `git push github`）。
