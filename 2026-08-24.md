## 2026-08-24 修复「08-22 起未来页乱码」(三次排查最终定论)

- **现象**：day2026-08-20/08-21 正常，08-22 起所有未来页显示 `\U0001f5a3`、`\u9996\u9875` 等字面量转义；用户重启 vocab-serve 后仍乱码。
- **误判历程**（记录教训）：
  1. 第1次判断：浏览器缓存/charset 问题 → 改 serve.py 加 `charset=utf-8` + `no-cache` + `?v=` 302 → 用户重启后**仍乱码**
  2. 第2次判断：客户端代理缓存（Data Saver无视no-cache）→ 加302跳转 → **仍乱码**
  3. 第3次（最终）：用 **raw bytes 正则 `rb'\x5cU[0-9a-fA-F]{8}'`** 扫描文件 → 发现 **25个字面量 `\U00XXXXXXXX`**（emoji转义）
- **真正根因**：`run_daily.py` PAGE 模板中 25 个 emoji 以 `\U00XXXXXXXX` 存储（🔓=\U0001f5a3, 🏠=\U0001f3e0 等）。上次修复只解码了4位`\uXXXX`（中文），**漏掉了8位`\U00XXXXXXXX`（emoji）**。gen_future.py 提取模板时原样继承 → 25个未来页每页25处字面量。
- **修复**：run_daily.py 模板中 `\U00XXXXXXXX` 全部解码为实际 UTF-8 emoji 字符；重生成 25 个受影响页 + calendar/review；全站31个day页面验证零转义残留。
- **提交**：`c6122f3`，已推送 backup/github。
- **铁律**：排查"乱码/改了没生效"必须用 raw bytes 扫描（`open('rb')` + `re.compile(rb'\x5cU...')`），Read工具/Grep会自动解码隐藏问题；模板里直接写UTF-8字符和emoji，不要用\u/\U转义。
