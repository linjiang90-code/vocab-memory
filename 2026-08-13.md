# 2026-08-13 工作日志

## 英语口语学习方案（词力专家）
- 需求：旅游 + 日常混合，每天 7 短句 + 3 长句，早 10:00 推送。
- 已创建自动化：`automation-1786592150095`（FREQ=DAILY;BYHOUR=10;BYMINUTE=0, ACTIVE）。
- 进度文件：`.workbuddy/memory/vocab-progress.json`（含 learnedSentences / reviewSchedule / 间隔复习 1/2/4/7/15/30 天）。
- 用户追加需求：中英文对照 + 生词难点注音标 + 原声伴读。
- 原声方案：用 edge-tts（en-US-AriaNeural，rate -10%）生成每句 mp3，安装在 `C:\Users\Win10\.workbuddy\binaries\python\envs\default`（venv）。生成脚本 `vocab-practice/gen_audio.py`。
- 练习页：`vocab-practice/day1.html`（中英对照 + 生词音标 chips + 🔊 伴读 / 🐢 慢速朗读，慢速用浏览器 SpeechSynthesis 兜底）。音频在 `vocab-practice/audio/s1..s10.mp3`。
- 后续：每天自动化推送新一批；生词注音已写入进度文件 keywords 字段。

## 增强版格式确认
- 用户追问 "Pardon?" 是否标准：结论分地区/场合——英式日常地道，美式偏正式易误会；唯一不出错是原句。以此为例定下增强版。
- 用户选定「完整增强版」：每句含 ① 简化/口语变体 ② 场景化地道表达(标场合+不同回答) ③ 语法提示。已写进自动化 prompt。
- 已将 Day 1 全部 10 句补成增强版（progress JSON 加 variants/scenes/grammar 字段；day1.html 重生成含三块新区块）。模板用于后续每日推送。

## 增加「发音提示」模块
- 用户要求标注连读/同化/滑音 与 清辅音浊化 等自然语流现象。
- 已为每句加 pron 字段（连读如 Could you→/kʊdʒu/ d+j 同化；浊化如 What do→/wʌ dʊ/ t→d）；progress JSON + day1.html 均已更新，day1.html 新增 🔤 发音提示块。
- 自动化 prompt 同步加入第⑤点「发音提示」。原声 mp3(rate -10%) 已自然体现连读/浊化，无需重生成。

## 系统重构：100 范式 · 5句/天 · 每30天+50 扩展
- 用户新需求：总结最常用 100 句范式（旅游+日常混合）；每天推 5 句；连续 20 天推完 100；30 天内可随机反复复习；每 30 天 +50 新句（100→150→200→…），新句随机重复学，以此类推。
- 已建主数据库 `vocab-practice/master.json`：meta(配置) + sentences(100 条，en/zh/theme/category/length/keyvocab[音标]/audio/learn{introduced,mastery,reviewCount,lastReviewed,enh})。前 10 条复用旧 Day1 句子（audio s1-s10 已存在）。
- 生成器：`gen_one.py`(单句音频) / `gen_audio_all.py`(批量补缺) / `gen_master_html.py`(总览页)。venv 同前。
- 总览页 `vocab-practice/master.html`：100 句可按 旅游/日常 + 分类 筛选，含生词音标与 🔊 原声。已修复 JSON 第102行 typo（fullIpa 多余引号）后生成成功。
- 示例页 `vocab-practice/day-demo.html`：新节奏「5句/天」样例（取 #1#2#3#4#8，含1长句），含完整增强五块。
- 自动化 `automation-1786592150095` 重写 prompt：5句/天、dayIndex<=20 顺序学新、>20 随机间隔复习、dayIndex==31/61/91 自动 +50 新句并生成音频、每句写回 enh(音标+变体+场景+语法+发音)、生成 day{today}.html、推送卡片。rrule 维持每日 10:00。
- 进行中：s11-s100 音频用 edge-tts 后台生成。

## 首次正式手动推送（Day 1）
- 用户要求现在手动跑一次正式推送（不等明天 10:00）。
- 因 s1-s10 在之前 Day-1 预览被标 introduced=true，为保持「20 天学完 100 句」干净节奏，push_day.py 先重置全部 learn 为未学，再取前 5 个未学句 = s1-s5（均为短句）作为首日新学。
- `push_day.py`：重置状态 → 写回 s1-s5 增强内容(复用 day1.html 成品 + 补 fullIpa 整句音标) → 标记 introduced/dueDate(2026-08-14 首复习)/mastery=1 → 生成 `day2026-08-13.html`（含顶部统计：第1天/今日新学5/累计5-100/总池100）。
- 校验：s1-s5 introduced=true，enh 已填，累计已学 5/100，audio/s1-s5.mp3 齐全，页面 13.9KB 生成成功。
- 说明：自动化明日 10:00 将推 Day 2 = s6-s10；第 21 天起进入随机复习。push_day.py 可复用（参数传日期）。

## 发音提示升级为完整语流音变体系
- 用户要求除浊化外，爆破音(失去爆破/不完全爆破)及其他发音变化也要标注。
- 完整覆盖：连读 linking、同化 assimilation(d+j→dʒ / t+j→tʃ / s+j→ʃ / n+j→ɲ)、滑音 glide(/w//j/插入)、清辅音浊化 voicing(t→d/p→b/k→g)、失去爆破 incomplete plosion(good boy/sit down/big city)、闪音 flap(better→/bɛɾɚ/ water→/wɔɾɚ/ party/city)、弱读 weak forms(the→/ðə/ to→/tə/ a→/ə/ you→/jə/ for→/fər/)、缩读 contractions(I'd=would / It's=is)、增音/脱落/鼻音同化等其他音变。
- 已更新自动化 prompt 第⑤点（影响今后所有新句）；并把 Day1(s1-s5) 的 pron 补成完整体系、重生成 day2026-08-13.html；push_day.py 模板标题与图例同步更新。
- 注：Day1 五句多为连读/同化/弱读/缩读/浊化；失去爆破与闪音在含 better/water/good boy/sit down 等词的句子（后续 s6-s100 中）才会出现，框架已就位会自动标注。

## master.html 增加「可点开发音详情」入口
- 用户要求总览页也能点开看任意句的完整发音标注。
- 重写 `gen_master_html.py`：每句卡片加「🔤 发音详情 ▾」可展开面板，渲染 enh 全字段（整句音标 / 发音提示 / 口语变体 / 场景用法 / 语法提示）；已推送句(#1-#5)显示完整内容并标 `ready` 高亮，其余 95 句显示「待生成」占位(#6-#100 在学习时自动补全)。
- 顶部新增图例(覆盖连读/同化/滑音/浊化/失去爆破/闪音/弱读/缩读)；工具栏加「🔤 展开全部发音详情」一键展开/收起。
- 已重生成 master.html（100 个详情面板就位，5 个完整 + 95 占位）。

## master.html 增加掌握度进度条 + 已学/未学筛选
- 用户要求每句加掌握度进度条，并支持按「已学/未学」筛选。
- 重写 gen_master_html.py：卡片读取 learn.introduced + learn.mastery，加 5 段式「掌握度」进度条（绿色段=已掌握档位，mastery/5）；卡片加 data-learned 属性。
- 工具栏新增「全状态/已学/未学」筛选分组，JS apply() 增加 learnFilter 维度（与主题、分类筛选叠加生效）。
- 已重生成 master.html：100 条均带进度条（s1-s5 各点亮 1 段），已学 5 / 未学 95，筛选按钮生效。

## 进度条颜色分级 + 顶部总进度
- 用户要求进度条按 0-2 红 / 3-4 黄 / 5 绿 分级上色，并加顶部总进度（已学 X/100 · 平均掌握度 Y/5）。
- 重写 gen_master_html.py：卡片 mbar/mwrap 按 mastery 等级加 lvl-red/lvl-yellow/lvl-green，CSS 分级着色（红 #dc2626 / 黄 #d97706 / 绿 #16a34a）；mnum 数字同步变色。
- 顶部新增 .stats 统计条：已学句数 learned/total、平均掌握度 avg(全池均值)/5、及一条总掌握度百分比进度条（gbar，按 avg 等级着色）。数据在 Python 端由 master.json 直接计算注入。
- 已重生成 master.html：100 卡当前全红档（mastery 0-1），已学 5/100、平均 0.1/5、总掌握度 1%。

## 平均掌握度改已学口径 + 总进度条联动筛选
- 用户要求：平均掌握度只看已学句；总进度条随「已学/未学」筛选联动。
- 重写 gen_master_html.py 统计段：avg 改为仅对 introduced=true 的句求均值（avg_learned），注入 `PCT_ALL`(全池均值%) 与 `PCT_LEARNED`(已学均值%) 两个常量。
- 顶部「平均掌握度」改为已学句口径（当前 1.0/5），并加「（已学句口径）」小字提示；新增 JS `updateStats()`，在 apply() 内调用，按 learnFilter 切换总进度条：全状态=PCT_ALL(1%)、已学=PCT_LEARNED(20%)、未学=0%，并按档位变色。
- 已重生成 master.html：平均掌握度 1.0/5；默认总掌握度 1%，切「已学」变 20%、切「未学」变 0%。
- 「已学 X/100」分母也改为随筛选变「当前可见句数」：全状态=5/100，已学=5/5，未学=0/95；与主题/场景筛选可叠加。
- 自动化 automation-1786592150095 已加第 9 步：每次推送收尾必须运行 venv 解释器 gen_master_html.py，自动重生成 master.html 与 master.json 同步（掌握度条/已学统计/总进度条/发音详情）。push_day.py 手动脚本也加了同步重生成步骤，已验证通过。
- 新增「一键预览入口」index.html（按钮进今日练习页+100句总览）+ 本地服务 serve.py（端口8765，标准库 http.server，GET 静态 + POST /api/mastery 回写掌握度到 master.json，已放开 CORS 可跨端口访问）。练习页(push_day.py TEMPLATE)加每句「✅认识+1/🟡模糊/🔴不认识-1」自评按钮，经 API 直接回写 mastery；master.html(gen_master_html.py)加「🔄同步掌握度」按钮+每15s自动拉取 API 实时刷新进度条。已验证：服务在线、POST id=1 clear→mastery2、unknown→mastery1。启动命令 python serve.py。

## 平均掌握度也随筛选联动
- 用户要求平均掌握度也跟着「已学/未学」筛选一起变（未学→0.0/5，已学→1.0/5）。
- 卡片 div 加 data-mastery 属性；重写 updateStats() 为遍历【当前可见卡片】实时计算：nL/sumL(已学句均值→stAvg)、nVis/sumVis(总掌握度%→stBar)；移除了静态 PCT_ALL/PCT_LEARNED 常量。
- 现三态：全状态 平均1.0/5·总1%；已学 平均1.0/5·总20%；未学 平均0.0/5·总0%。总掌握度与平均掌握度均随筛选（含主题/分类）联动。

## 自动化加「推送前确保服务运行」+ 总览页可折叠
- 用户要求：① 把本地回写服务 serve.py 加进自动化，每次 10:00 推送前自动确保服务在运行；② 总览页顶部「发音详情说明」段落和所有筛选分组（全部/旅游/日常、已学/未学、各分类按钮）改成可隐藏、可折叠。
- 自动化 automation-1786592150095 新增第 0 步：推送前 curl 检查 http://127.0.0.1:8765/api/status，不可达则用 venv 解释器 serve.py 以后台(run_in_background)启动端口8765，约3s后继续；已运行则跳过。
- gen_master_html.py 改造：发音说明改为原生 `<details class="legend-box">`（默认收起，点 summary 展开）；筛选分组（主题/已学未学/分类）包进 `<div id="filtersBody" class="collapsed">`，加「🔍 筛选条件」按钮 `toggleFilters()` 切换折叠（默认收起），展开/收起文字随状态切换。CSS 加 legend-box/filtersBody.collapsed 样式。顶部统计与「展开全部/同步掌握度」按钮常驻不受折叠影响。
- 已重生成 master.html，校验通过（legend details 默认关、filtersBody 默认 collapsed、toggleFilters 就位）。

## 筛选折叠记忆 + 语速 0.75/0.5 可选
- 用户要求：①「筛选条件」折叠状态记忆（localStorage，默认折叠）；② 语速 0.75x、0.5x 可选。
- gen_master_html.py：toggleFilters 改为 setFilterCollapsed(c)，读写 localStorage `vocab_filters_collapsed`（null 时默认 true=折叠）；工具栏加「语速」select(1.0/0.75/0.5)，applySpeed(v) 设所有 `.card audio` 的 playbackRate 并持久化 `vocab_speed`，加载时回读。
- push_day.py TEMPLATE：移除原 🐢慢速按钮(bool slow)，改为「语速」select + setSpeed(v)（localStorage `vocab_speed`）；playAudio 设 a.playbackRate=speed；speak/next(全部伴读) u.rate=speed。原声 mp3 与浏览器 TTS 朗读均受语速控制。
- 已重生成 day2026-08-13.html + master.html，校验通过（speed select/选项/持久化、filter 持久化默认折叠、old slow 已移除）。

## 总览页循环跟读 + 每句独立语速微调
- 用户要求：① 总览页加「同一句多遍循环（如连放 3 遍跟读）」；② 语速选择做成「每句独立微调」。
- gen_master_html.py：每句卡片 audio 下加 readwrap 控件（🔁跟读 select 2/3/5/8 遍 + ▶开始/⏹停止按钮 + 语速 select 1.0/0.75/0.5/0.4x）；JS 新增 bindCard() 给每句绑定：spdSel 改时设 audio.playbackRate 并持久化 localStorage `vocab_speed_<id>`，加载时回读（回退全局 `vocab_speed`）；readBtn 点击连放 reps 遍（onended 自动续播，再点停止），循环时用该句独立语速。顶部「语速」改为「语速(批量预设)」，applySpeed(v) 批量设所有句 playbackRate + 写每句 localStorage + 同步各 spdSel，加载时不再强制覆盖独立值。
- push_day.py TEMPLATE 同步：卡片加 `<audio id="au-<id>">` + readwrap（repsSel/rp-<id>、readBtn/rb-<id>、spdSel）；JS 加 cardSpeed 映射 + loopRead/onLoopEnd/stopLoop/setCardSpeed；playAudio 改为复用 au- 音频元素（rate=cardSpeed||全局）；setSpeed 升级为批量预设（遍历 .card audio 与 .spdSel 同步、写每句 localStorage）。原声 mp3 跟读循环与每句独立语速即生效，speak/全部伴读仍走全局语速。
- 已重生成 master.html 校验通过（readwrap/readBtn/spdSel/bindCard/批量 applySpeed 均就位）；练习页 day2026-08-13.html 仍为旧模板，push_day.py 新代码将于下次每日推送自动生效（不改动现有掌握度数据）。本地服务 serve.py 已在后台重启（端口8765）供预览与回写。

## Git 版本控制（防误删）
- 用户要求用 Git 防误删：在 vocab-practice 初始化本地仓库（纯本地版本库，无 remote）。
- git init + 局部配置 user.name=VocabCraft / user.email=agent@workbuddy.local；创建 .gitignore（排除 __pycache__/*.pyc/*.bak/.workbuddy 等）与 .gitattributes（* text=auto 忽略 Windows 换行差异，*.mp3 binary）。
- 首次基线提交 9027e48（113 文件：master.json + 全部 html/py 脚本 + audio/s1-s100.mp3 共 100 个原声）；规范化提交 7132941（.gitattributes）。工作区干净。
- 新建自动化 automation-1786609176614「vocab-practice 每日 Git 快照」（DAILY 23:00，有变更才提交带日期快照，无变更跳过，禁 amend/force/reset）。约定：今后每次对本项目做改动立即 git commit 纳入历史。

## 练习页发音详情折叠 + 自评进度条
- 用户要求：① 今日练习页「🔤 发音详情」可折叠、默认隐藏；② 自评掌握度显示。
- push_day.py TEMPLATE：发音块改 `class="block pr collapsible collapsed"`（点击标题 toggleBlock 展开/收起，默认 collapsed）；自评区加彩色进度条 `.mbar>.mfill`（0-2红/3-4黄/5绿，mColor 分级），assess 后 setMbar 实时刷新宽度+颜色。
- 已重生成 day2026-08-13.html + master.html，commit 1bf04a0（2 文件 +97/-11）。掌握度数据未变（s1-s5 仍各 1/5）。

## 练习页四块详情合并单按钮全折叠(默认全隐藏)
- 用户要求：变体/场景/语法/发音详情 合并成一个按钮「全折叠/全隐藏」，默认全隐藏。
- push_day.py TEMPLATE：四块包进 `<div class="details collapsed" id="dt-<id>">`，前置「📂 展开详情」按钮(每卡 toggleDetails + 全局 toolbar 按钮 toggleAllDetails 一键全展开/全收起)；默认 collapsed 隐藏。移除上一版的 pron 单块折叠(collapsible/toggleBlock)。
- 已重生成 day2026-08-13.html + master.html，commit 75f92f3（2 文件 +32/-24）。

## 练习页详情折叠持久化 + 跟读改原声播放
- 用户要求：① 折叠/展开状态记 localStorage 刷新保持；② 底部「跟读」改「原声播放多次」(语速仍可调)。
- push_day.py TEMPLATE：setCardDetails 写 localStorage `vocab_details_<id>`(open/closed)；新增 applyStoredDetails() 渲染后回读并套用(仅 open 才展开，默认 collapsed)；toggleAllDetails 仍逐句写回；顶部全局按钮文字随状态同步。readwrap 标签「🔁 跟读」→「🔁 原声播放」(loopRead 本就播 au- 原声 mp3 多遍，行为不变)。
- 已重生成 day2026-08-13.html + master.html，commit 6eb0e46（2 文件 +14/-4）。

## 修复展开全部 + 底部改语音朗读
- 用户反馈：① 顶部「展开全部详情」点了没反应；② 底部「原声播放」不要跟读，改为原文句子语音读。
- fix1：toggleAllDetails 原 `const show = !anyCollapsed` 逻辑反转（全隐藏时反算成收起），改为 `const show = anyCollapsed`（有折叠才展开全部）。
- fix2：底部 readwrap 标签「🔁 原声播放」→「🔁 朗读」；loopRead/stopLoop(基于 au- mp3 循环) 整体替换为 loopSpeak（SpeechSynthesis 朗读英文原句 N 遍，u.rate=cardSpeed||speed，onend 自动续播，再点停止）。顶部 🔊 仍播录制原声(au-)。
- 已重生成 day2026-08-13.html + master.html，commit 588259c（2 文件 +26/-38）。
